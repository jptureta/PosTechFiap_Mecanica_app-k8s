apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: oficina
  labels:
    app: redis
    app.kubernetes.io/name: oficina
spec:
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
  selector:
    app: redis
  type: ClusterIP

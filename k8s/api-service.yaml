apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: oficina
  labels:
    app: api
    app.kubernetes.io/name: oficina
spec:
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30000
      protocol: TCP
      name: http
  selector:
    app: api
  type: NodePort
